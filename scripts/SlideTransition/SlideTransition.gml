/// @function						slide_transition(mode, target);
/// @param {Real}			_mode	TRANS_MODE to be in to start the transition
/// @param {Asset.GMRoom}	_target	target room
/// @return {undefined}

function SlideTransition(_mode, _target)
{
	with (oTransition) {
		mode = _mode;
		target = _target;
	}
}